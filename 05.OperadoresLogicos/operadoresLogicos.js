/*
    Operadores lógicos
    && AND
    || OR
    !  NOT
*/

const nombre = 'Armando';
const edad = 17;
const tieneEntrada = true;
const tienePermiso = true;

// Ejemplo #1 - &&
// // const permitirAcceso = edad >= 18 && tieneEntrada;
// const permitirAcceso = edad >= 18 && tieneEntrada ? 'SI' : 'NO';
// console.log('Puede entrar al concierto:', permitirAcceso);

// Ejemplo #2 - ||
// const permitirAcceso = (edad >= 18 && tieneEntrada) || (tienePermiso && tieneEntrada) ? 'SI':'NO';
// console.log('Puede entrar al concierto:', permitirAcceso);

// Ejemplo #3 -- !
// const variable = true;
// console.log(!variable);